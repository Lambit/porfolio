class Projects {
    constructor(id, title, links, details) {
        this.id = id;
        this.title = title;
        this.links = links;
        this.details = details;
    };
};

export default Projects;