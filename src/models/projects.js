class Projects {
    constructor(id, title, img, links, details) {
        this.id = id;
        this.title = title;
        this.img = img;
        this.links = links;
        this.details = details;
    };
};

export default Projects;